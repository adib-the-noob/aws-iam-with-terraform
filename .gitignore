# Local .terraform directories
**/.terraform/*

# Terraform state files
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Sensitive variable files
*.tfvars
*.tfvars.json

# Override files (can contain local config)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc

# Plan files (output of `terraform plan -out=...`)
*.tfplan

# Lock file — ignore if using per-environment or team workflows
# (keep it if you want reproducible provider versions)
# .terraform.lock.hcl

# IDE and OS stuff
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
